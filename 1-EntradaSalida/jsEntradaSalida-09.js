/*
Debemos lograr tomar el importe por ID ,
transformarlo a entero (parseInt), luego
mostrar el importe con un aumento del 10 %
en el cuadro de texto "RESULTADO".*/
/*function mostrarAumento()
{
    let sueldo;
    let porcentaje;
    let resultado;
    let mensaje;

    sueldo=document.getElementById('txtIdSueldo').value;
    sueldo=parseInt(sueldo);

    porcentaje= sueldo * 10 / 100;

    resultado=sueldo + porcentaje; 

    mensaje="El importe total con un aumento del 10% es "+resultado;

    document.getElementById('txtIdResultado').value=mensaje;

}
*/


/*
9 BIS- Debemos lograr tomar el importe por ID ,
transformarlo a entero (parseInt), luego
mostrar el importe con un aumento X%
en el cuadro de texto "RESULTADO".*/
/*function mostrarAumento()
{
    let sueldo;
    let porcentaje;
    let aumento;
    let resultado;
    let mensaje;

    sueldo = document.getElementById('txtIdSueldo').value;
    sueldo = parseInt(sueldo);

    porcentaje = prompt("Ingrese el porcentaje requerido");

    aumento = sueldo * porcentaje/100;

    resultado = sueldo + aumento;

    mensaje="El total con el "+porcentaje+"% de aumento es "+resultado;

    document.getElementById('txtIdResultado').value=mensaje;
}*/

